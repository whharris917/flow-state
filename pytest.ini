[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    slow: tests that exercise Numba warmup or real physics integration
filterwarnings =
    ignore::DeprecationWarning
